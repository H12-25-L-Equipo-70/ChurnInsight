# ============================================================================
# CHURNINSIGHT ENVIRONMENT VARIABLES
# ============================================================================
# Copia este archivo a .env y completa con tus valores
# NO COMMITEAR ARCHIVO .env A GIT (usar solo localmente)
# ============================================================================

# ============================================================================
# ORACLE DATABASE - WALLET CONFIGURATION
# ============================================================================

# Contraseña del usuario admin en Oracle ADB
ORACLE_DB_PASSWORD=your_oracle_password_here

# Ruta absoluta al directorio del wallet (descomprimido)
# Linux/Mac:
# ORACLE_WALLET_PATH=/home/ronaldorv/Repositorios/ChurnInsight/backend/wallet_pymer
# Windows:
ORACLE_WALLET_PATH=C:\Repositorios\ChurnInsight\backend\wallet_pymer

# Sistema propiedad para TNS_ADMIN
# Linux/Mac:
# ORACLE_NET_TNS_ADMIN=/home/ronaldorv/Repositorios/ChurnInsight/backend/wallet_pymer
# Windows:
ORACLE_NET_TNS_ADMIN=C:\Repositorios\ChurnInsight\backend\wallet_pymer

# Contraseña de la wallet (normalmente "wallet_password")
WALLET_PASSWORD=wallet_password

# ============================================================================
# SPRING BOOT CONFIGURATION
# ============================================================================

# Perfil activo (dev, prod)
SPRING_PROFILES_ACTIVE=dev

# Puerto del servidor
SERVER_PORT=8080

# Contexto de la aplicación
SERVER_SERVLET_CONTEXT_PATH=/api/v1

# ============================================================================
# LOGGING
# ============================================================================

# Nivel de log global (INFO, DEBUG, WARN, ERROR)
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_COM_PYMER_CHURNINSIGHT=DEBUG

# ============================================================================
# DATABASE POOL CONFIGURATION
# ============================================================================

# Tamaño mínimo del pool
SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE=5

# Tamaño máximo del pool
SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE=30

# Timeout de conexión (ms)
SPRING_DATASOURCE_HIKARI_CONNECTION_TIMEOUT=30000

# Timeout de inactividad (ms)
SPRING_DATASOURCE_HIKARI_IDLE_TIMEOUT=900000

# ============================================================================
# FEIGN CLIENT (AI SERVICE)
# ============================================================================

# URL base del servicio FastAPI (AI)
FEIGN_CLIENT_CONFIG_AISERVICE_URL=http://localhost:8000

# Timeout de conexión (ms)
FEIGN_CLIENT_CONFIG_DEFAULT_CONNECT_TIMEOUT=5000

# Timeout de lectura (ms)
FEIGN_CLIENT_CONFIG_DEFAULT_READ_TIMEOUT=30000

# ============================================================================
# PRODUCTION CONFIGURATION (si aplica)
# ============================================================================

# SSL/TLS para HTTPS (solo en producción)
# SERVER_SSL_ENABLED=true
# SERVER_SSL_KEY_STORE=/app/config/keystore.p12
# SERVER_SSL_KEY_STORE_PASSWORD=keystore_password
# SERVER_SSL_KEY_STORE_TYPE=PKCS12

# ============================================================================
# ACTUATOR ENDPOINTS
# ============================================================================

# Health check
MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS=when-authorized

# Prometheus metrics
MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=health,info,metrics,prometheus
